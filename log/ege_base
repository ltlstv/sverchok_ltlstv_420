# 2 Задание
  print('x,y,z,w')
  print(f'сумма = {x}')
  for i in range(1,100,2): (start,stop,step)
  for i in s: где (s - список)
  if (not((y<=w) or (z<=w)) or not(z))==False: 
  (<=) = -->
  or - или
  and - и
  == - сравнимо
  if not(sp): (sp - список) при отсутствии элементов будет 0, при наличии 1;
  
# 5 Задание
  a=bin(5)[2:0] (первые 2 символа не нужны, так это хлам, который мы срезаем)
  int('10120301230',4) (str,система счисления строки) - перевод в 10-тичную.
  num.count('1') - найдет количество едениц в списке или строке num
  break - оборвать цикл
  continue - пропускает итерацию
  pass - ниче не делает, но прога работает
# 6 Задание 
  import turtle*
  left(градус), right(градус), forward(длинна вперед)
  pu() - поднять перо
  dot(R) - точка с радиусом R
  goto(x,y) - переместить черепаху на данные координаты
  done() - смерть черепахи
# 8 Задание
  for a in range(7) - генерация цифр в числе
  num=str(a) + str(b) + str(c) + str(d) + str(e) - генерация числа
  spisok.index('6') - номер эелемента списка
  len(spisok) длина списка
  10 % 2 =0
  10 // 2 = 5
  spisok[0] - получение элемента списка
  from itertools import product
  nums=product('01234567',repeat=5)
  spisok.split() - создание листа из строчки
  ''.join(spisok) - создание монолитной строчки из листа
  spisok=[] - создание списка
  spisok.append(что-нибудь)
# 12 Задание  
  if all(x<123 for x in spisok)
# 16 Задание 
  import sys
  sys.setrecursionlimit(2500)
  def f(n):
    return n*f(n-1)
# 17 Задание
  with open('17.txt') as f:
    nums=[int(x) for x in f] - чтение многострочного файла
    n=f.readline() - однострочное чтение
    a=abs[-1] - модуль данного параметра
    max(1,2,4,5,1,231,23,77) - макс число из параметов
    min(183,9123,3,4,1,2,3,93,123) - мин число из параметров
    ** - возведение в степень
# 23 Задание 
  count += 1 (count = count + 1)
# 24 Задание
  s=s.replace('old','new',сколько раз)
  num[start:stop:step] - срез
  num[::-1] чтение списка/строки с конца
# 26 Задание
  spisok=sorted(spisok, reverse=True)
  s.sort(reverse=False)    тоже самое, что и    s.sort()         (МЕТОДО СПИСКА)
# 27 Задание 
  a=a+a - метод слияния списков
